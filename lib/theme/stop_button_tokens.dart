import 'package:flutter/material.dart';

/// Design tokens — stop recording button (glass sky-blue disc).
abstract final class StopButtonTokens {
  static const defaultSize = 72.0;

  /// Frosted sky-blue fill.
  static const fillColor = Color(0x99C8E4FF);

  /// Soft outer ring.
  static const borderColor = Color(0xB3E3F2FF);

  /// Rich saturated stop icon.
  static const iconColor = Color(0xFF2B5CE6);

  static const previewBackground = Color(0xFFF4F5F7);
}
