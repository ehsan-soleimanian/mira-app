import 'package:flutter/material.dart';

/// Design tokens — Figma `618:3447` stop recording button.
abstract final class StopButtonTokens {
  static const defaultSize = 72.0;
  static const fillColor = Color(0xFFEFF2F7);
  static const iconColor = Color(0xFF4B6EF5);
  static const previewBackground = Color(0xFFF4F5F7);
}
