import 'package:flutter/material.dart';

/// Figma-aligned tokens for the memory graph screen.
abstract final class GraphTokens {
  static const nodeFill = Color(0xFF2B2B2B);
  static const edge = Color(0xFFD6D6D6);
  static const newMemoryBadge = Color(0xFFFFE8D9);
  static const sheetCard = Color(0xFFF7F2EA);
  static const sheetBarrier = Color(0x1A000000);
  static const nodeRadius = 18.0;
}
