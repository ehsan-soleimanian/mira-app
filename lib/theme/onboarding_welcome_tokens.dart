/// Layout tokens — Figma onboarding welcome `724:4804`.
abstract final class OnboardingWelcomeTokens {
  static const designWidth = 393.0;
  static const designHeight = 852.0;

  /// Top illustration zone (~65% of frame).
  static const illustrationFlex = 65;
  static const panelFlex = 35;

  /// Hero art — shifted up so orb sits near the status bar (Figma y = -62).
  static const illustrationLeft = -14.0;
  static const illustrationTop = -105.0;
  static const illustrationWidth = 428.0;
  static const illustrationArtHeight = 720.0;

  /// Greeting inside the speech bubble (overlay on art only).
  static const bubbleTextTop = 255.0;
  static const bubbleTextHorizontal = 48.0;

  /// Bottom content panel — solid background.
  static const panelHorizontal = 24.0;
  static const panelTopPadding = 20.0;
  static const titleSubtitleGap = 10.0;
  static const buttonBottomPadding = 70.0;
}
